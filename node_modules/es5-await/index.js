"use strict";

var stack = [];

function _async(func, args) {
  var iter = func.apply(this, args);
  stack.push(iter);
  iter.next();
}

// For Express user
function _asyncroute(func) {
  return function(req, resp, next) {
    try {
      return _async(func, arguments);
    } catch (ex) {
      console.log(ex);
    }
  }
}

function* _await() {
  var iter = stack.pop()

  Promise.all(arguments).then(function*(value){
    stack.push(iter)
    try {
      var next;
      if (value.length === 1) {
        next = yield iter.next(value[0]);
      } else {
        next = yield iter.next(value);
      }
      if (next.done && stack[stack.length] === iter) {
        stack.pop();
      }
    } catch (ex) {
      if (next.done && stack[stack.length] === iter) {
        stack.pop();
      }
      throw ex; //?
    }
  }).catch(function*(error) {
    stack.push(iter)
    var next;
    try {
      var next;
      next = yield iter.next(error);
      if (next.done && stack[stack.length] === iter) {
        stack.pop();
      }
    } catch (ex) {
      if (next.done && stack[stack.length] === iter) {
        stack.pop();
      }
      throw ex;
    }
  });
  return yield;
}

// Promify standard node async calbacks(error, data){...}
function _promify(args) {
  var output = [];
  // await(asyncFunction, arg0, ...)
  if (typeof args[0] === "function") {
    output[0] = _c2p(args);
    return output;
  }
  // await(... , ..., [asyncFunction, arg0, ...], ..., [...] so on)
  for (var i = 0; i < args.length; i++) {
    if (args[i] instanceof Array) {
      output[i] = _c2p(args[i]);
    } else {
      output[i] = args[i];
    }
  }
  return output;
}

function _c2p(args) {
  console.log(args)
  var self = this;
  var func = args[0];
  console.log("ffffffffffffffffffffffffffffffffff")
  console.log(func)
  var output = [];
  for (var i = 1; i < args.length; i++) {
    output.push(args[i]);
  }
  return new Promise(function(ok, err) {
    output.push(function(error, data) {
      if (error) {
        err(error)
      } else {
        ok(data);
      }
    })
    func.apply(self, output);
  });
}

module.exports = {
  await: _await,
  async: _async,
  asyncroute: _asyncroute,
  c2p: _c2p
};
